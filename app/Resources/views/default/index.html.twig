{% extends 'base.html.twig' %}

{% block slider %}
    <div style="height: 360px;">
        <div id="carousel" class="carousel slide" data-ride="carousel">
            <!-- Indicators -->
            <ol class="carousel-indicators">
                {% for key,ads in sliderAds %}
                    {% if key == 0 %}
                        <li data-target="#carousel" data-slide-to="0" class="active"></li>
                    {% else %}
                        <li data-target="#carousel" ta-slide-to="1"></li>
                    {% endif %}
                {% endfor %}
            </ol>

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                {% for key,ads in sliderAds %}
                    {% if key == 0 %}
                        <div style="height: 390px" class="item active">
                            <img src="{{ asset(' uploads/images/advert/'~ ads.banner) }}" alt="nice image"
                                 style="height: 390px; width: 659px"/>

                        </div>

                    {% else %}
                        <div style="height: 390px" class="item ">
                            <img src="{{ asset(' uploads/images/advert/'~ ads.banner) }}" alt="REDEEM LOGO"
                                 style="height: 390px;width: 659px"/>

                            <div class="carousel-caption">
                                <h1>{{ ads.heading }}</h1>
                            </div>
                        </div>
                    {% endif %}

                {% endfor %}
            </div>

            <!-- Controls -->
            <a class="left carousel-control" href="#carousel" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>
{% endblock %}


  {% block torchLogo %}
      <div style="height: 290px;">
          <img src="{{ asset('logo.png') }}" alt="REDEEM LOGO" style="height: 260px;width: 650px"/>
      </div>
  {% endblock %}

  



{% block badge %}
    {% for key,news in topNews %}
        <div class="col-md-3">
            <div style="height:100px" class="wrapper">
                <a href="/news/{{ news.category.name }}/{{ news.id }}">
                    <div class=" pull-left">
                        <img alt="embeded image" src="{{ asset(' uploads/images/news/'~ news.file) }}"
                             style="padding-right: 15px" width="150px" height="90px"/>
                    </div>
                    <div style="width:auto; color: #161f29;padding-top: 15px; padding-left:  10px">
                        <strong> {{ news.title |title }}</strong>
                    </div>
                </a>
            </div>
        </div>

    {% endfor %}

{% endblock %}




{% block moreNews %}
    <div class="col-sm-3">
        <div class="editorialheadline">
            <h3>More News</h3>
            <ul>
                {% for news in moreNews %}
                    <li><a href="/news/{{ news.category.name }}/{{ news.id }}">
                            <span>{{ news.title |capitalize }}</span>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}

{% block moreTestimonies %}
    <div class="col-sm-3">
        <div class="editorialheadline">
            <h3> More Testimonies </h3>
            <ul>
                {% for testimony in moreTestimony %}
                    <li><a href="/testimony/{{ testimony.id }}">{{ testimony.title |title}} </a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
	          

{% block moreNotice %}
    <div class="col-sm-3">
        <div class="editorialheadline">
            <h2><small> Notice </small></h2>
                  <ul>
                      {% for notice in moreNotice %}
                          <li><a href="/notice/{{ notice.id }}"> {{ notice.title |title }} </a></li>
                      {% endfor %}
                  </ul>
        </div>
    </div>
{% endblock %}

{% block mostRead %}
    <div class="col-sm-3">
        <div class="editorialheadline">
            <h3><small> Most Viewed </small></h3>
               <ul>
                   {% for view in mostView %}
                       <li><a href="/news/{{ view.category.name }}/{{ view.id }}"> {{ view.title |title }} </a></li>
                   {% endfor %}
               </ul>
        </div>
    </div>
{% endblock %}

              {% block newsNav %}

                  <ol class="breadcrumb-blue">
                      <li class="active"><a href="/news"> <strong> News ></strong></a></li>
                      {% for category in  newsCategories %}
                          <li><a href="/news/{{ category.id }}">{{ category.name }}</a></li>
                      {% endfor %}

                  </ol>
              {% endblock %}

          
              {% block newsBadgeRight %}
                  {% for key, news in xRightNews %}

                      <div style=" margin: 5px;height: 322px;" class="wrapper">

                          <a href="/news/{{ news.category.name }}/{{ news.id }}">
                              <img src="{{ asset(' uploads/images/news/'~ news.file) }}" height="200px"
                                   width="285px" style="padding-bottom: 10px" alt="embeded Image"/>

                              <div style="width:auto; color: #161f29;padding-top: 15px; padding-left:  10px">
                                  <strong> {{ news.title |title }}</strong>
                              </div>

                          </a>
                          <br/>

                          <div class="pull-right" style="width:auto;color: #b4bcc2; padding-right: 25px;">
                              <strong>{{ news.category.name }}</strong>
                          </div>
                      </div>

                  {% endfor %}
              {% endblock %}
	          
	          {% block newsStoriesRight %}
                  <div style=" margin: 5px; " class="wrapper">
                      {% for key, newsStories in RightNews %}

                          <div style="height:100px" class="wrapper-underline">
                              <a href="/news/{{ newsStories.category.name }}/{{ newsStories.id }}">
                                  <div class=" pull-left">
                                      <img alt="embeded image"
                                           src="{{ asset(' uploads/images/news/'~ newsStories.file) }}"
                                           style="padding-right: 15px" width="150px" height="90px"/>
                                  </div>
                                  <div style="width:auto; color: #161f29;">
                                      <strong> {{ newsStories.title |title }} </strong>
                                  </div>
                              </a>
                              <br/>

                              <div class="pull-right" style="width:auto;color: #b4bcc2; padding-right: 25px;">
                                  <strong>{{ newsStories.category.name }}</strong>
                              </div>
                          </div>


                      {% endfor %}
                  </div>

              {% endblock %}
	          
	          {% block newsStoriesLeft %}
                  <div style=" margin: 5px; " class="wrapper">
                      {% for key, newsStories in LeftNews %}



                          <div style="height:100px" class="wrapper-underline">
                              <a href="/news/{{ newsStories.category.name }}/{{ newsStories.id }}">
                                  <div class=" pull-left">
                                      <img alt="embeded image"
                                           src="{{ asset(' uploads/images/news/'~ newsStories.file) }}"
                                           style="padding-right: 15px" width="150px" height="90px"/>
                                  </div>
                                  <div style="width:auto; color: #161f29;">
                                      <strong> {{ newsStories.title |title }} </strong>
                                  </div>
                              </a>
                              <br/>

                              <div class="pull-right" style="width:auto;color: #b4bcc2; padding-right: 25px;">
                                  <strong>{{ newsStories.category.name }}</strong>
                              </div>
                          </div>


                      {% endfor %}
                  </div>
              {% endblock %}
	          
	          {% block newsBadgeLeft %}

                  {% for key, news in xLeftNews %}

                      <div style=" margin: 5px;height: 322px;" class="wrapper">

                          <a href="/news/{{ news.category.name }}/{{ news.id }}">
                              <img src="{{ asset(' uploads/images/news/'~ news.file) }}" height="200px"
                                   width="285px" style="padding-bottom: 10px" alt="embeded Image"/>

                              <div style="width:auto; color: #161f29;padding-top: 15px; padding-left:  10px">
                                  <strong> {{ news.title |title }}</strong>
                              </div>

                          </a>
                          <br/>

                          <div class="pull-right" style="width:auto;color: #b4bcc2; padding-right: 25px;">
                              <strong>{{ news.category.name }}</strong>
                          </div>
                      </div>


                  {% endfor %}

              {% endblock %}



			 {% block testimonyNav %}
                 <ol class="breadcrumb-red">
                     <li><strong> Testimonies ></strong></li>
                 </ol>
             {% endblock %}




	          {% block testimonyBadge %}

                  {% for testimony in testimonies %}
                      <div class="col-md-3">
                      <div style=" margin: 5px;height: 322px;" class="wrapper">

                          <a href="/testimony/{{ testimony.id }}">
                              <img src="{{ asset(' uploads/images/testimony/'~ testimony.image) }}" height="200px"
                                   width="275px" style="padding-bottom: 10px" alt="embeded Image"/>

                              <div style="width:auto; color: #161f29;padding-top: 15px; padding-left:  10px">
                                  <strong> {{ testimony.title |title }}</strong>
                              </div>

                          </a>
                          <br/>

                          <div class="pull-right" style="width:auto;color: #b4bcc2; padding-right: 25px;">
                              <strong></strong>
                          </div>
                      </div>
                      </div>
                  {% endfor %}

              {% endblock %}